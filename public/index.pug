mixin icon(name)
	- mod = mod || ''
	svg(class="icon icon-" + name)
		use(xlink:href="symbol/sprite.svg#" + name)

html
	head
		meta(charset="utf8")
		link(rel="stylesheet" href="style.css")
		meta(name="viewport" content="width=device-width, initial-scale=1")
	body
		header.ada
